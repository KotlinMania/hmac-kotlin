# Rust callers — `hmac-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `hmac` | `hmac` | 20 | 7 | 5 | 6 |

## `hmac`

### Inbound symbols (5 distinct, 20 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `hmac`.

#### `hmac::Hmac` — 8 site(s) (3 codex, 5 other, 7 caller crates)

- ✅ `codex-kotlin/codex-app-server` `tests/suite/v2/connection_handling_websocket.rs:23` — `use hmac::Hmac`
- ✅ `codex-kotlin/codex-app-server-transport` `src/transport/auth.rs:468` — `use hmac::Hmac`
- ✅ `codex-kotlin/codex-cloud-requirements` `src/lib.rs:28` — `use hmac::Hmac`
- `age-kotlin/age` `src/primitives.rs:4` — `use hmac::Hmac`
- `aws-sigv4-kotlin/aws-sigv4` `benches/hmac.rs:5` — `use hmac::Hmac`
- `aws-sigv4-kotlin/aws-sigv4` `src/sign/v4.rs:5` — `use hmac::Hmac`
- `oauth2-kotlin/oauth2` `examples/letterboxd.rs:17` — `use hmac::Hmac`
- `zip-kotlin/zip` `src/aes.rs:11` — `use hmac::Hmac`

#### `hmac::Mac` — 8 site(s) (3 codex, 5 other, 7 caller crates)

- ✅ `codex-kotlin/codex-app-server` `tests/suite/v2/connection_handling_websocket.rs:24` — `use hmac::Mac`
- ✅ `codex-kotlin/codex-app-server-transport` `src/transport/auth.rs:469` — `use hmac::Mac`
- ✅ `codex-kotlin/codex-cloud-requirements` `src/lib.rs:29` — `use hmac::Mac`
- `age-kotlin/age` `src/primitives.rs:4` — `use hmac::Mac`
- `aws-sigv4-kotlin/aws-sigv4` `benches/hmac.rs:5` — `use hmac::Mac`
- `aws-sigv4-kotlin/aws-sigv4` `src/sign/v4.rs:5` — `use hmac::Mac`
- `oauth2-kotlin/oauth2` `examples/letterboxd.rs:17` — `use hmac::Mac`
- `zip-kotlin/zip` `src/aes.rs:11` — `use hmac::Mac`

#### `hmac::digest::FixedOutput` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `aws-sigv4-kotlin/aws-sigv4` `benches/hmac.rs:4` — `use hmac::digest::FixedOutput`
- `aws-sigv4-kotlin/aws-sigv4` `src/sign/v4.rs:5` — `use hmac::digest::FixedOutput`

#### `hmac::digest::CtOutput` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `age-kotlin/age` `src/primitives.rs:4` — `use hmac::digest::CtOutput`

#### `hmac::digest::MacError` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `age-kotlin/age` `src/primitives.rs:4` — `use hmac::digest::MacError`
