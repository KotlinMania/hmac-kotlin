# Port targets — `hmac-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `hmac` that codex-rs uses, and therefore the minimum subset that `hmac-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-59] (rank `155`, tier `direct`, status `To Do`)

**Cargo package name**: `hmac`
**Rust module name (in `use` statements)**: `hmac`

## Summary

- **2** distinct symbols imported by codex-rs
- **6** total `use` statements
- **3** codex source files affected
- **3** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `hmac::Hmac`

- **3 `use` site(s)** across **3 codex member crate(s)**: `codex-app-server`, `codex-app-server-transport`, `codex-cloud-requirements`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-transport/src/transport/auth.rs:468` (`codex-app-server-transport`) — `use hmac::Hmac`
- `app-server/tests/suite/v2/connection_handling_websocket.rs:23` (`codex-app-server`) — `use hmac::Hmac`
- `cloud-requirements/src/lib.rs:28` (`codex-cloud-requirements`) — `use hmac::Hmac`

### `hmac::Mac`

- **3 `use` site(s)** across **3 codex member crate(s)**: `codex-app-server`, `codex-app-server-transport`, `codex-cloud-requirements`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-transport/src/transport/auth.rs:469` (`codex-app-server-transport`) — `use hmac::Mac`
- `app-server/tests/suite/v2/connection_handling_websocket.rs:24` (`codex-app-server`) — `use hmac::Mac`
- `cloud-requirements/src/lib.rs:29` (`codex-cloud-requirements`) — `use hmac::Mac`
