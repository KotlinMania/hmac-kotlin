# Port report — `hmac-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `hmac`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 20 `use` sites from 5 other repos
- **Outbound**: 4 `use` sites to 1 other repos
- **Unresolved external**: 2 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `hmac` | `hmac` | 20 | 1 | COD-59 | 155 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (20 edges, 7 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `aws-sigv4-kotlin` | `aws-sigv4` | 6 |  |
| `age-kotlin` | `age` | 4 |  |
| `codex-kotlin` | `codex-app-server` | 2 | ✅ |
| `codex-kotlin` | `codex-app-server-transport` | 2 | ✅ |
| `codex-kotlin` | `codex-cloud-requirements` | 2 | ✅ |
| `oauth2-kotlin` | `oauth2` | 2 |  |
| `zip-kotlin` | `zip` | 2 |  |

### Symbol-level demand (5 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `hmac::Hmac` | 8 | 3 | 5 | 7 |
| `hmac::Mac` | 8 | 3 | 5 | 7 |
| `hmac::digest::FixedOutput` | 2 | 0 | 2 | 1 |
| `hmac::digest::CtOutput` | 1 | 0 | 1 | 1 |
| `hmac::digest::MacError` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 5 symbols)

##### `hmac::Hmac` (8 sites, 3 codex, 5 other, 7 caller crates)

Codex call sites:

- `codex-kotlin/codex-app-server` `tests/suite/v2/connection_handling_websocket.rs:23` — `use hmac::Hmac`
- `codex-kotlin/codex-app-server-transport` `src/transport/auth.rs:468` — `use hmac::Hmac`
- `codex-kotlin/codex-cloud-requirements` `src/lib.rs:28` — `use hmac::Hmac`

Other workspace call sites:

- `age-kotlin/age` `src/primitives.rs:4` — `use hmac::Hmac`
- `aws-sigv4-kotlin/aws-sigv4` `benches/hmac.rs:5` — `use hmac::Hmac`
- `aws-sigv4-kotlin/aws-sigv4` `src/sign/v4.rs:5` — `use hmac::Hmac`
- `oauth2-kotlin/oauth2` `examples/letterboxd.rs:17` — `use hmac::Hmac`
- `zip-kotlin/zip` `src/aes.rs:11` — `use hmac::Hmac`

##### `hmac::Mac` (8 sites, 3 codex, 5 other, 7 caller crates)

Codex call sites:

- `codex-kotlin/codex-app-server` `tests/suite/v2/connection_handling_websocket.rs:24` — `use hmac::Mac`
- `codex-kotlin/codex-app-server-transport` `src/transport/auth.rs:469` — `use hmac::Mac`
- `codex-kotlin/codex-cloud-requirements` `src/lib.rs:29` — `use hmac::Mac`

Other workspace call sites:

- `age-kotlin/age` `src/primitives.rs:4` — `use hmac::Mac`
- `aws-sigv4-kotlin/aws-sigv4` `benches/hmac.rs:5` — `use hmac::Mac`
- `aws-sigv4-kotlin/aws-sigv4` `src/sign/v4.rs:5` — `use hmac::Mac`
- `oauth2-kotlin/oauth2` `examples/letterboxd.rs:17` — `use hmac::Mac`
- `zip-kotlin/zip` `src/aes.rs:11` — `use hmac::Mac`

##### `hmac::digest::FixedOutput` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `aws-sigv4-kotlin/aws-sigv4` `benches/hmac.rs:4` — `use hmac::digest::FixedOutput`
- `aws-sigv4-kotlin/aws-sigv4` `src/sign/v4.rs:5` — `use hmac::digest::FixedOutput`

##### `hmac::digest::CtOutput` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `age-kotlin/age` `src/primitives.rs:4` — `use hmac::digest::CtOutput`

##### `hmac::digest::MacError` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `age-kotlin/age` `src/primitives.rs:4` — `use hmac::digest::MacError`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `sha2-kotlin` | 4 | 4 | `sha2` | COD-135 | 23 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `digest` | 104 | 57 | `hmac` |
| `streebog` | 2 | 2 | `hmac` |
